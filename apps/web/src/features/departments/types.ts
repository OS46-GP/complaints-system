export interface Department {
  id: string;
  name: string;
  subAuthority: string | null;
  createdAt: string;
}

export interface ApiDepartment {
  id: string;
  name: string;
  subAuthority: string | null;
  createdAt: string;
}
