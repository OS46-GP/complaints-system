meta {
  name: Create User
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/users
  body: json
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "username": "new_official",
    "password": "securePass123",
    "role": "Official"
  }
}

script:post-response {
  const data = res.getBody();
  if (data.id) {
    bru.setVar("userId", data.id);
  }
}
