meta {
  name: Get User
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/users/{{userId}}
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq(200)
  res.body.id: eq({{userId}})
  res.body.username: $notNull
  res.body.role: $notNull
}
