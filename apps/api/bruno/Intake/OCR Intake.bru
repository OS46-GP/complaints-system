meta {
  name: OCR Intake
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/intake/ocr
}

headers {
  Authorization: Bearer {{token}}
}

body:multipart-form {
  image: @file(_files/test-complaint.jpg)
}

assert {
  res.status: eq(201)
  res.body.fields: $notNull
  res.body.imageUrl: $notNull
}
