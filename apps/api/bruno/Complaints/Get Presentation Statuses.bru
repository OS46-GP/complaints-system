meta {
  name: Get Presentation Statuses
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/complaints/presentation-statuses
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq(200)
  res.body: $notNull
  res.body.length: gt(0)
}
