meta {
  name: Get Reception Methods
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/complaints/reception-methods
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq(200)
  res.body: $notNull
  res.body.length: gt(0)
}
