meta {
  name: Get Complaint Types
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/complaints/complaint-types
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq(200)
  res.body: $notNull
  res.body.length: gt(0)
}
