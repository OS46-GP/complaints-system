meta {
  name: Unlink Complaint
  type: http
  seq: 19
}

delete {
  url: {{baseUrl}}/complaints/{{complaintId}}/links/{{linkedComplaintId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq(200)
  res.body.unlinked: eq(true)
}
