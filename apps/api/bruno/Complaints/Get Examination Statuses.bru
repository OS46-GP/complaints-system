meta {
  name: Get Examination Statuses
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/complaints/examination-statuses
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq(200)
  res.body: $notNull
  res.body.length: gt(0)
}
