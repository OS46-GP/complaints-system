meta {
  name: Analyze (AI Triage)
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/complaints/{{complaintId}}/analyze
  body: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq(201)
  res.body.severity: in(["LOW","MEDIUM","HIGH"])
  res.body.recurrenceMatches: $notNull
}
