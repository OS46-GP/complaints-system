meta {
  name: Get Complaint
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/complaints/{{complaintId}}
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq(200)
  res.body.id: eq({{complaintId}})
  res.body.subject: $notNull
  res.body.complaintNumber: $notNull
}
