meta {
  name: Update Profile
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/profile
  body: json
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "fullName": "محمد أحمد"
  }
}

assert {
  res.status: eq(200)
  res.body.fullName: eq(محمد أحمد)
}
