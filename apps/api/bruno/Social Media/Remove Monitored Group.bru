meta {
  name: Remove Monitored Group
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/social/groups/{{monitoredGroupId}}/remove
  body: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq(200)
  res.body.id: $notNull
}