meta {
  name: Trigger Poll
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/social/poll
  body: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq(201)
  res.body.polled: eq(true)
}