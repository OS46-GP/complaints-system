meta {
  name: Delete Letter Template (Cleanup)
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/letter-templates/{{templateId}}
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq(200)
  res.body.success: eq(true)
}