meta {
  name: Get Letter Settings
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/letter-settings
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq(200)
  res.body.id: eq(1)
  res.body.organizationNameAr: $notNull
}